# AI-KART Frontend — Environment Variables
# Copy to .env.local and fill in values

# Backend API base URL (leave empty to use Vite proxy, set for production)
VITE_API_BASE_URL=

# AI Hub is not configured here.
# Paste the one-line shopbot.js script into index.html when this storefront is connected.
